import tkinter as tk
from doctest import master
from importlib.resources import contents
from tkinter.constants import RIGHT
from tkinter.constants import LEFT
from tkinter.ttk import Label
from tkinter import ttk
from ctypes import windll

root = tk.Tk()

root.title("24009620's Calculator DEMO")

windll.shcore.SetProcessDpiAwareness(2)

Name = tk.Label(text="2400620's Calculator")
Name.pack()

ttk.Label(root, text='classic label').pack()

output = ttk.Label(root, text=input, )
output.config(fg="green")

Zero = ttk.Button(master, text='0',)
Zero.pack(padx=20, pady=10, ipadx=50,ipady=70, side=LEFT)

One = ttk.Button(master, text='1',)
One.pack(padx=20, pady=10, ipadx=50,ipady=70, side=LEFT)

Two = ttk.Button(master, text='2',)
Two.pack(padx=20, pady=10, ipadx=50,ipady=70, side=LEFT)

root.geometry('650x1000+20+20')

root.mainloop()
